# 🛠️ Developer Guide: R‑Taaw Backend

Welcome to the developer documentation for the **R‑Taaw** project — a minimalist, multilingual recipe book backend built on Django.

This guide outlines the structure, conventions, and tools used in the project. Please read it carefully before contributing.

---

## 📁 Project Structure

```text
r_taaw_backend/
├── cookbook/              # Core app: recipes, ingredients, tags, translations
│   ├── models/
│   ├── serializers/
│   ├── views/
│   └── ...
│
├── locale/                # Django .po translation files
│
├── middlewares/           # Custom middleware (e.g. localization)
│   └── localization.py
│
├── nginx.conf/
│   └── default.conf
│
├── r_taaw_backend/
│   ├── __init__.py
│   ├── asgi.py
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
│
├── scripts/               # Utility scripts: testing, translations, etc.
│   ├── test.sh
│   ├── coverage.sh
│   └── make_translations.sh
│
├── users/                 # User management: auth, profile, etc.
│
├── .env.example           # Environment configuration template
├── Makefile               # Common development commands
├── Makefile.local.example # Optional local overrides for Makefile
├── docker-compose.yaml
├── Dockerfile
├── gunicorn.conf.py
├── LICENSE
├── manage.py
├── Pipfile
├── Pipfile.lock
├── pyproject.toml
├── README.md
├── TRANSLATION.md
└── DEVELOPER_GUIDE.md      ← you are here
```

---

## 🧭 Planned Architecture

- **Backend**: Django + DRF
- **Frontend**: Flutter Web and Android app
- **PostgreSQL**: Main database
- **Redis**: Caching + Celery broker
- **Celery**: Asynchronous tasks (e.g., background translation, email)
- **Docker**: Containerized deployment
- **Gunicorn + Nginx**: Production-ready web server

---

## 🧪 Local Development

1. Clone the repo:

```bash
git clone https://github.com/ivangolubykh/r_taaw_backend.git
cd r_taaw_backend
```

2. Set up the environment:

```bash
cp .env.example .env
mkdir -p .volumes/db_data .volumes/logs static
chmod ugo=rwx .volumes/logs
```

3. Build & run:

```bash
docker compose build --no-cache
docker compose up
```

4. Access:

- API Documentation: `http://localhost/api/docs/`
- Admin: `http://localhost/admin/`

---

## 📦 API Documentation

Interactive documentation is available at:

```text
/api/docs/
```

It is auto-generated by **drf-spectacular** based on serializers and viewsets.

---

## 🔤 Translation

See: [`TRANSLATION.md`](TRANSLATION.md)

Covers:

- How to add new languages
- How to extract strings
- How to manage recipe translations
- LibreTranslate usage

---

## 📂 Static Files

Static assets (collected for admin panel, etc.) live in `/static/`.\
Use the standard Django workflow:

```bash
python manage.py collectstatic
```

---

## ✅ Code Conventions

- Use `black .` and `isort .` for formatting
- Use `flake8` for checking format
- Use `gettext_lazy` for all static text
- Follow DRF’s serializer/viewset conventions

---

## 🧪 Testing

Tests are located in each app’s `tests/` folder.

You can run tests in various ways:

- Simple test run:

  ```bash
  make test
  ```

- Test with coverage report:

  ```bash
  make test-cov
  ```

- View last coverage report:

  ```bash
  make coverage
  ```

Optional override logic is supported through `Makefile.local` (see `Makefile.local.example`).

---

## 🤝 Contributing

We welcome contributions!

Please follow these steps:

1. **Find or open an issue** describing the task you want to work on.
2. **Comment that you are working on it**, to avoid duplication.
3. Wait for feedback or approval (if it’s a new proposal).
4. **Fork this repository**, and create a new branch based on the latest `dev`.
5. Name your branch clearly (e.g. `add-i18n-support`).
6. When you open a Pull Request:
   - Target the `dev` branch, not `main`
   - Add a descriptive title, such as `[#42] Add i18n support for user profiles`
   - In the PR description, include a line like:\
     `Closes #42` — this will automatically close the issue when the PR is merged.
7. Keep your PR focused and atomic. One task — one PR.

🔐 Only one person should work on a task at a time.\
Please always claim a task by commenting on the corresponding issue.

📌 **Note:** If your change is significant, open an issue first to discuss it before starting work.

---

## 🚫 Not Planned

The following features will **not** be part of this project:

- WebSockets
- Public-facing API versioning
- 2FA (two-factor authentication)
- IP allowlists for admin access

---

## 🛟 Need Help?

Contact the repository owner via GitHub:\
[@ivangolubykh](https://github.com/ivangolubykh)

---
